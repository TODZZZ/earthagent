// Copy this file to config.js and add your API key
const config = {
  // Enter your OpenAI API key here
  OPENAI_API_KEY: ""
};

// Do not modify below this line
try {
  if (typeof module !== 'undefined') {
    module.exports = config;
  }
} catch (e) {
  // Ignore export error in browser context
} 